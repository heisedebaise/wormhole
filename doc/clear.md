# 清理过期文件

通过配置[clear.json](../conf/clear.json)文件可设置自动清理过期文件，可以配置多个目录：

```json
[{
    "Path":"目录",
    "Timeout":"超时时长，单位：秒。"
}]
```

`Timeout`属性可以使用以下单位简化配置：

- d 天
- h 小时
- m 分钟

如：`1d2h3m4`=`1*24*60*60 + 2*60*60 + 3*60 +4`。

> 自动清理时间间隔为`1`分钟。
